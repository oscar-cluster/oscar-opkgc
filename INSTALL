Installing opkgc
================

Run
$ ./autogen.sh (if running from SVN checkout)
$ ./configure
$ make
$ make install

Dependancies
------------

* Python 2.4+ (not tested with other versions)
* Python header and library files (usually python-devel package)
* lxml
* Cheetah templating system
